export const Paths = {
  login: "/",
  view: "/view-client",
  add: "/add-client",
};

export enum MessageTypes {
  INFO = "info",
  SUCCESS = "success",
  WARNING = "warning",
  ERROR = "error",
  DEFAULT = "default",
}

export enum TabName {
  VIEW = "VIEW",
  ADD = "ADD",
}
